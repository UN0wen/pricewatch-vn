enum Routes {
    Account = "/account",
    Home = "/",
    Item = "/item/:itemID",
    Login = "/login",
    Logout = "/logout",
    SignUp = "/signup",
    Profile = "/profile"
}

export default Routes